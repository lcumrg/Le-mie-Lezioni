# Config per i build automatici da git.
# Header e redirect vivono in public/_headers e public/_redirects
# (finiscono in dist/ e valgono anche per i deploy manuali).

[build]
  command = "npm run build"
  publish = "dist"
